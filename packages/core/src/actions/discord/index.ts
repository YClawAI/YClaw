/**
 * Public API re-exports for the Discord action module.
 *
 * Import from here instead of the individual sub-modules:
 *   import { DiscordExecutor } from './discord/index.js';
 */

export { DiscordExecutor } from './executor.js';
